/**
 * Глобальні клієнтські стори (Zustand).
 * Серверний стан — TanStack Query (queryKeys у @/lib/query-keys).
 * URL-стан — nuqs (searchParams).
 */
export { useAuthModalStore } from "./use-auth-modal";

/**
 * Глобальні (спільні) типи додатку.
 * Типи, специфічні для фіч, зберігати в features/<name>/types.ts або types/index.ts.
 */

// Приклад: базові типи для пагінації чи API
// export type PaginationParams = { page: number; limit: number };

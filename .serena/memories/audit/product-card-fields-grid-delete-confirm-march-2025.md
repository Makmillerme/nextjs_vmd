Product card audit fixes: (1) hiddenOnCard now hides ALL widget types on card (dynamic-field-renderer). (2) DynamicTabs always uses computeGridLayout for real card + preview; non-preview empty cells = hidden sm:block placeholders so col spans match editor. Remove field X only when previewMode && onRemoveField. (3) ProductDocumentsTab: AlertDialog before deleting file; locales productDocuments.confirmDeleteTitle/Description. (4) FieldGridEditor + ProductCardPreviewModal: AlertDialog before removing field from tab; locales tabsConfig.confirmRemoveFieldTitle/Description. Verify: ESLint on touched files (product-detail-sheet, product-documents-tab, field-grid-editor, product-card-preview-modal, dynamic-field-renderer) passes.
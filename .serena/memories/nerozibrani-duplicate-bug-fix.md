Дублікати «Нерозібрані» з order=0 виникали, коли знімали галочку «За замовчуванням» у PATCH: у групі не лишалось isDefault=true, а ensureDefaultStatus при GET accounting-groups створював новий рядок. Виправлення: (1) ensureDefaultStatus тепер шукає існуючий рядок з code=nerozibrani і виставляє йому isDefault=true замість create; (2) PATCH /api/admin/statuses/[id] забороняє body.isDefault===false якщо existing.code==='nerozibrani'; (3) у statuses-management Checkbox для isDefault disabled, коли code nerozibrani і значення true, з підказкою mandatoryDefaultLocked. Зайві дублікати користувач може видалити (кнопка видалення для рядків без isDefault).
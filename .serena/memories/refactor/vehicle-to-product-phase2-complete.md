Phase 2 complete (2026-02-23). Types and hooks: VehicleConfigTab->ProductConfigTab, VehicleConfigTabField->ProductConfigTabField, VehicleConfigResponse->ProductConfigResponse, useVehicleConfig->useProductConfig. Created use-product-config.ts, deleted use-vehicle-config.ts. API product-config now returns productType (not vehicleType). ListConfig.vehicleType->productType. vehicle-detail-sheet: productTypeId prop, useProductConfig, productConfig. vehicles-page: productTypeId={listConfig?.productType?.id}. dynamic-field-renderer: ProductConfigTabField. Build passes.
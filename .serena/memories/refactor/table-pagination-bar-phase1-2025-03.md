Phase 1 UI unification: `TablePaginationBar` in `src/components/table-pagination-bar.tsx`. Props: page, totalPages, pageInputValue, handlers, goToPage, pageSize, pageSizes, onPageSizeChange, optional isReady (default true), isLoading, pageSizeMenuContentClassName. Uses `useLocale` for common.pagination keys. Consumers: statuses-management, field-definitions-management, tabs-config-management, users-management, products-page (isReady=mounted, isLoading=loading, pageSize display uses pageSizeClamped vs DEFAULT). ESLint + tsc clean.
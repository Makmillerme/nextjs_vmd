1) ProductDocumentsTab: added previewMode prop. When previewMode && productId=0, shows folder structure (from documentFolders) with empty docs and disabled AddTile. 2) FolderSection: added Folder icon (amber), addDisabled prop, improved styling (shadow-sm, bg-card, bg-muted/10 content). 3) AddTile: added disabled prop - prevents upload when in preview. 4) DynamicFieldRenderer file_upload: always uses ProductDocumentsTab when showFiles (product.id>0 || previewMode), passes previewMode={previewMode && product.id===0}. Removed separate saveProductFirst placeholder for preview. 5) Deleted prisma/seed-product-cms.ts.
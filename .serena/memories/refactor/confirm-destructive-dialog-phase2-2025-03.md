Phase 2: `ConfirmDestructiveDialog` wraps shadcn AlertDialog — props: open, onOpenChange, title, description?, children?, errorMessage?, cancelLabel, confirmLabel, confirmPendingLabel?, confirmPending?, cancelDisabled?, onConfirm (void|Promise), confirmTone default|destructive (default destructive), contentSize, contentClassName. Confirm uses preventDefault. Migrated management + products confirm flows; `ban-user-dialog` still imports `AlertDialogBody` for form slot. Direct `AlertDialog` usage outside `confirm-destructive-dialog` + `ui/alert-dialog` should be rare; add new confirms via this component.
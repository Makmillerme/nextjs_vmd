Status accounting groups (statuses-management.tsx GroupCard): removed the between-card chain connector UI entirely (the flex justify-center py-1 block that showed nextGroupId linkage). It caused layout height to change during reorder/optimistic updates and made the list jump. Database nextGroupId unchanged. Also collapsed GroupCard wrapper from nested relative+card to a single rounded-lg border bg-card root div.
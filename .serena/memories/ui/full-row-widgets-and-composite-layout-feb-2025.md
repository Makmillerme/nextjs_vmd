1) Tab config full-row widgets (media_gallery, file_upload, composite, textarea): only Row input shown, Section hidden. They occupy entire row. When adding: order=(floor(maxOrder/3)+1)*3 to place on own row. 2) Composite layout: removed grid option. CompositeLayout = row|column. Row layout uses grid-cols-3 (3 per row, auto-wrap). Removed gridColumns/gridRows UI. parseCompositePresetValues: grid falls back to row. 3) Files: tabs-config-management FULL_ROW_WIDGETS, addField special order for full-row, updateFieldPlacement handles section=1 for full-row.
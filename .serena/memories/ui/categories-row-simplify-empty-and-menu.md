`categories-management.tsx`: при 0 типів не показується лічильник «N типів»; порожній стан під категорією (текст «немає типів» + лінк Додати) прибрано — при розгортанні без типів лишається лише `border-t` блок без рядків. Dropdown (⋯) на рядку категорії видалено як дубль Sheet; дії — через клік по назві (редагування), Plus — додати тип, стрілки — порядок.

`categories-management.tsx`: рядок категорії як StatusRow: reorder (стрілки), `#{cat.order}`, ghost Plus → `openVtCreate(cat.id)`; без DropdownMenu на рядку категорії. Якщо `types.length===0`: спейсер `w-5`, без «N типів», плюс reorder + order + Plus. Якщо є типи — Collapsible, «N типів», той самий хвіст. Рядок типу: без DropdownMenu; без нейтральної крапки; назва, бейдж, «N од.», `#{vtIdx+1}`; клік → `openVtEdit`. Футер Sheet категорії: outline «Додати тип» + видалити.

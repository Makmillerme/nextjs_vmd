`CategoriesManagement`: дерево категорій обгорнуте як секція статусів — `rounded-lg border bg-card`, шапка `border-b bg-muted/30 rounded-t-lg` з іконкою `Layers` та `productsConfig.categoriesConfig.treeTitle`, тіло `flex flex-col`; `Collapsible` з `border-b last:border-b-0`; рядок категорії `py-2` як у StatusRow; вкладений блок типів `border-t border-border bg-muted/20 divide-y divide-border`. Локалі uk/en + `npm run generate:locales`.

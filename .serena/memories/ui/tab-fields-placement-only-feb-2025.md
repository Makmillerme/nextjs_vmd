TabsConfigManagement: simplified assigned fields UI. Removed ColSpan, Required, Section (title), ProductType from tab field cards. Now only Row and Section (рядок, секція) for placement. Encoding: order = (row-1)*100 + (section-1). Decoding: row=floor(order/100)+1, section=(order%100)+1 for order>=100; else row=1, section=order+1. Save uses defaults: colSpan=1, isRequired=false, productTypeId=null, sectionTitle=null. Locale: productsConfig.tabsConfig.row, section. Removed Checkbox, Select imports and typesForCategory.
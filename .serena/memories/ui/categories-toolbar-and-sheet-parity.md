`categories-management.tsx`: прибрано блок «Порядок» з Sheet категорії (лишились імя, іконка, опис); `catOrder` лишається в стейті для create/save. Outline «+» біля пошуку видалено; кнопка додавання категорії — ghost `size-7` + `Plus` `size-3.5` у шапці картки (`rounded-t-lg`) як у воронці статусів. Рядок категорії: кнопки як `StatusRow` — `size-6`, Chevron `size-3`, без `z-10`; рядок типу — меню `size-6` + `MoreHorizontal` `size-3`. Іконки папок Folder/FolderOpen прибрані.

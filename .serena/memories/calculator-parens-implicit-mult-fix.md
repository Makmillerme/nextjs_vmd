Калькулятор: виправлено 'Помилка' для дужок та неявного множення.

1. normalizeInput: додано нормалізацію повноширинних дужок .replace(/\uFF08/g, "(") та .replace(/\uFF09/g, ")"), щоб вирази з буфера обміну (U+FF08, U+FF09) коректно парсились.

2. evaluate (Shunting yard): при openParen прибрано виклик flushOps() перед opStack.push(t). Раніше оператор (наприклад *) виносився в RPN до отримання другого операнда з дужок, що давало некоректний RPN і NaN. Тепер при ( лише збільшується parenDepth і в стек кладеться (; оператори виносяться при ) у flushOps(true) та в кінці в flushOps(). Вирази типу 25(6-9) та (2+3) обчислюються коректно.